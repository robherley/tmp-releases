name: Test

on:
  push:
    tags:
      - "*"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Prentend to release
        run: |
          echo "pretend to release..."
          sleep 5
          echo "released!"
